import React from "react";

const Calcula = () => {
  const resultado = "";

  return (
    <div>
      <h1>Calculadora</h1>
      <input placeholder="Primeiro valor" />
      <input placeholder="Qual operação" />
      <input placeholder="Segundo valor" />
      <p>Resultado:{resultado}</p>
    </div>
  );
};

export default Calcula;
